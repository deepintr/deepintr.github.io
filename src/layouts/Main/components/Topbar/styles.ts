import { Styles } from 'react-jss';

const styles: Styles = {
  root: {
    background: 'inherit',
  },
  ghButton: {
    marginBottom: '-0.2rem',
    '&:hover': {
      background: 'none !important',
    },
  },
};

export default styles;
